<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
		<%- include( 'layouts/head'); -%>
  </head>
<body>
    <div class="container">
        <%- include( 'layouts/navbar', {nav: "<li><a href='/books'>Books</a></li><li class='active'><a href='/authors'>Authors</a></li><li><a href='/publishers'>Publishers</a></li><li><a href='/genres'>Genres</a></li>"}); -%>
        <h1><%= title %></h1>
            <%if (authors.length> 0) { %>
                <table class="table table-striped table-hover ">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>First Name</th>
                            <th>Last Name</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% i=1 %>
                            <% (authors).forEach(function(author){ %>
                                <%if (typeof author !='undefined' ) { %>
                                    <tr>
                                        <td><%=i ++ %></td>
                                        <td><%=author.firstName%></td>
                                        <td><%=author.lastName %></td>
                                    </tr>
                                <% } %>
                            <% });%>
                    </tbody>
                </table>
            <% } %>
    </div>
</body>

</html>
