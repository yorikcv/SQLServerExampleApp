<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
		<%- include( 'layouts/head'); -%>
  </head>
<body>
    <div class="container">
        <%- include( 'layouts/navbar', {nav: "<li><a href='/books'>Books</a></li><li><a href='/authors'>Authors</a></li><li class='active'><a href='/publishers'>Publishers</a></li><li><a href='/genres'>Genres</a></li>"}); -%>
        <h1><%= title %></h1>
            <%if (publishers.length> 0) { %>
                <table class="table table-striped table-hover ">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Name</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% i=1 %>
                            <% (publishers).forEach(function(publisher){ %>
                                <%if (typeof publisher !='undefined' ) { %>
                                    <tr>
                                        <td><%=i ++ %></td>
                                        <td><%=publisher.name%></td>
                                    </tr>
                                <% } %>
                            <% });%>
                    </tbody>
                </table>
            <% } %>
    </div>
</body>

</html>
